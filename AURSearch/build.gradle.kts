version = "1.1.0"
description = "Adds an aur slash command to search the AUR with."

aliucord {
    changelog.set(
            """
                # 1.1.0
                * Updated for v101.3
            """.trimIndent()
    )
}