version = "1.2.0"
description = "Adds a ccmoddb slash command to search the CrossCode mod database."

aliucord {
    changelog.set(
            """
                # 1.2.0
                * Updated for v101.3
            """.trimIndent()
    )
}